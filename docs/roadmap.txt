features/
├── animation-parsing/      ← GSAP animations
├── preset-resolution/      ← GSAP presets
├── css-animations/         ← CSS animations (nueva)
├── svg-animations/         ← SVG animations (nueva)
└── physics-animations/     ← Physics (nueva)



Soporte para callbacks en sintaxis @


 Stagger automático para listas
<div *ngFor="let item of items" animateLoad="fadeIn({ stagger: 0.1 })">


LLEVARME EL CODIGO PARA PACKAGE FOLDER (ver ejemplos en GitHub) y hacer una navegación para las animaciones en la web de docs
Publicar como npm package
